<!-- W  frattext.tex          GrpConst documentation             Bettina Eick -->
<Chapter Label="The Frattini Extension Method">
<Heading>The Frattini Extension Method</Heading>
<Index>The Frattini Extension Method</Index>
<P/>
This is a method to construct up to isomorphism the soluble groups
of a given order. The main function <C>FrattiniExtensionMethod</C> to
construct groups is described in Section <Ref Sect="The Main Frattini Extension Function"/>.
<P/>
The construction process consists of two parts which can be addressed
separately. In the first step a list of possible candidates for the
Frattini factors of the desired groups is determined up to isomorphism.
See Section <Ref Sect="The Construction of Frattini Free Groups"/> for the
corresponding functions. In the second step the determined candidates
are considered one after the other and for each candidate a list of
extensions is computed. See Section <Ref Sect="The Determination of Frattini Extensions"/> for the available functions.
<Section Label="The Main Frattini Extension Function">
<Heading>The Main Frattini Extension Function</Heading>
<Index>The Main Frattini Extension Function</Index>
<ManSection>
<Func Name="FrattiniExtensionMethod" Arg="order[, flags][, uncoded]"/>
<Description>
First we describe the <E>input</E> of the function. The <A>order</A> is the
size of the desired groups. The optional input <A>uncoded</A> is a
boolean which determines the output format. If it is <K>true</K>, then
pc groups are returned. Otherwise, if it is <K>false</K> or not given,
then code records describing pc groups are returned (see
<C>PcGroupCodeRec</C>).
<P/>
The optional input <A>flags</A> is a record which is used to restrict
the construction process to groups with certain properties only.
This record consists of any of the following
entries:
<List>
<Mark><C>nilpotent</C></Mark>
<Item>must be <K>true</K>. Only nilpotent groups are constructed.</Item>
<Mark><C>nonnilpot</C></Mark>
<Item>must be <K>true</K>. Only non-nilpotent groups are constructed.</Item>
<Mark><C>supersol</C></Mark>
<Item>must be <K>true</K>. Only supersoluble groups are constructed.</Item>
<Mark><C>nonsupsol</C></Mark>
<Item>must be <K>true</K>. Only non-supersoluble groups are constructed.</Item>
<Mark><C>pnormal</C></Mark>
<Item>must be a list of primes. Only groups with a normal Sylow <M>p</M>-subgroup
      for all <M>p</M> in the given list are constructed.</Item>
<Mark><C>nonpnorm</C></Mark>
<Item>must be a list of primes. Only groups without a normal Sylow <M>p</M>-subgroup
      for all <M>p</M> in the given list are constructed.</Item>
</List>
If a particular entry is not set, then no restriction on the
groups is assumed. The default is an empty record of flags. Any
combination of flags is possible. However, not all combinations make
sense; for example, if <C>nilpotent</C> and <C>nonnilpot</C> are both true,
then the algorithm will return the empty list. If <C>nonnilpot</C> is
true and <C>pnormal</C> is the list <M>[3]</M>, then the non-nilpotent groups
whose Sylow 3-subgroup is normal will be computed.
<P/>
The <E>output</E> of the function is usually a list of pc groups or code
records depending on <A>uncoded</A>. However, it may happen that the output
list contains not only pc groups or codes, but also lists of pc groups
or codes. This means that the groups in such a sublist are probably
non-isomorphic, but the algorithm did not do a final verification, since
this would be time-consuming. If desired, then the user might do
a verification using the function <C>DistinguishGroups</C> described
below.
<P/>
Moreover, it might be worth noting that the groups in such sublists
of the output list are always reduced by the random isomorphism test
(see the Section on <C>Random Isomorphism Testing</C> in the reference manual).
Hence the probability that there are still isomorphisms between
groups in this list is less than <M>2^{-100}</M>.
<Example><![CDATA[
gap> flags := rec( nonnilpot := true, pnormal := [3] );
rec( nonnilpot := true, pnormal := [ 3 ] )
gap> grps := FrattiniExtensionMethod( 24, flags, true );
[ <pc group of size 24 with 4 generators>, 
  <pc group of size 24 with 4 generators>, 
  <pc group of size 24 with 4 generators>, 
  <pc group of size 24 with 4 generators>, 
  <pc group of size 24 with 4 generators>, 
  <pc group of size 24 with 4 generators>, 
  <pc group of size 24 with 4 generators> ]
gap> List( last, IdGroup );
[ [ 24, 1 ], [ 24, 5 ], [ 24, 8 ], [ 24, 6 ], [ 24, 7 ], [ 24, 4 ], 
  [ 24, 14 ] ]
]]></Example>
<Example><![CDATA[
gap> FrattiniExtensionMethod( 8 );
[ rec( code := 323, extdim := [ 2, 2 ], first := [ 1, 1, 2 ], 
      isFrattiniFree := false, isUnique := true, order := 8, 
      socledim := [ 1 ] ), 
  rec( code := 34, extdim := [ 2 ], first := [ 1, 1, 3 ], 
      isFrattiniFree := false, isUnique := true, order := 8, 
      socledim := [ 1, 1 ] ), 
  rec( code := 36, extdim := [ 2 ], first := [ 1, 1, 3 ], 
      isFrattiniFree := false, isUnique := true, order := 8, 
      socledim := [ 1, 1 ] ), 
  rec( code := 2343, extdim := [ 2 ], first := [ 1, 1, 3 ], 
      isFrattiniFree := false, isUnique := true, order := 8, 
      socledim := [ 1, 1 ] ), 
  rec( code := 0, extdim := [  ], first := [ 1, 1, 4 ], 
      isFrattiniFree := true, isUnique := true, order := 8, 
      socledim := [ 1, 1, 1 ] ) ]
]]></Example>
</Description>
</ManSection>
</Section>
<Section Label="The Construction of Frattini Free Groups">
<Heading>The Construction of Frattini Free Groups</Heading>
<Index>The Construction of Frattini Free Groups</Index>
<P/>
A finite group is called <E>Frattini free</E> if it has a trivial
Frattini subgroup. As candidates for the Frattini factors of
the groups of size <A>order</A>, we compute Frattini free groups of
suitable size dividing <A>order</A>.
<ManSection>
<Func Name="FrattiniFactorCandidates" Arg="order, flags[, uncoded]"/>
<Description>
The input is similar to the input for the function
<C>FrattiniExtensionMethod</C>.
<P/>
The output is a list of candidates for the Frattini factors of the
desired groups, i.e. the groups of size <A>order</A> possibly restricted
by <A>flags</A>. By default the groups are returned as codes which may
be changed using the boolean <A>uncoded</A>.
<P/>
Note that the computed list is always reduced to isomorphism type
representatives. Moreover, it might happen that some of the Frattini
free groups are not realised as Frattini factors of a group of size
<A>order</A>. However, in practice this is a very rare case.
<P/>
Furthermore, note that for this part of the Frattini extension method
the restriction to the positive properties <C>nilpotent</C>, <C>supersol</C> and
<C>pnormal</C> in the flags record will reduce the amount of computation
considerably, while the negative properties do not have such a major
influence on the efficiency of this method.
<Example><![CDATA[
gap> flags := rec( nonsupsol := true );
rec( nonsupsol := true )
gap> FrattiniFactorCandidates( 24, flags, true );
[ <pc group of size 24 with 4 generators>, 
  <pc group of size 12 with 3 generators>, 
  <pc group of size 24 with 4 generators> ]
gap> List(last, IdGroup);
[ [ 24, 12 ], [ 12, 3 ], [ 24, 13 ] ]
]]></Example>
</Description>
</ManSection>
</Section>
<Section Label="The Determination of Frattini Extensions">
<Heading>The Determination of Frattini Extensions</Heading>
<Index>The Determination of Frattini Extensions</Index>
<P/>
A group <M>H</M> is a <E>Frattini extension</E> of a group <M>G</M> if there
exists a normal subgroup <M>N</M> of <M>H</M> such that <M>H/N \cong G</M> and
<M>N \leq \phi(H)</M> holds. Clearly, each finite group can be obtained
as a Frattini extension of a Frattini free group.
<ManSection>
<Func Name="FrattiniExtensions" Arg="code/group, order[, uncoded]"/>
<Description>
Here the default input is a Frattini free group described by a code
and the size <A>order</A> of the groups which shall be constructed.
Alternatively, one can input a Frattini free group as pc group.
Moreover, it is possible to give a list of codes or pc groups at
once. The flag <A>uncoded</A> changes the output format to pc groups
instead of codes as above.
<P/>
The output of this function is similar to the output of the
function <C>FrattiniExtensionMethod</C>.
<Example><![CDATA[
gap> G := SmallGroup( 24, 12 );
<pc group of size 24 with 4 generators>
gap> FrattiniSubgroup(G);
Group([  ])
gap> FrattiniExtensions( G, 48, true );
[ <pc group of size 48 with 5 generators>, 
  <pc group of size 48 with 5 generators>, 
  <pc group of size 48 with 5 generators> ]
gap> List( last, IdGroup);
[ [ 48, 29 ], [ 48, 30 ], [ 48, 28 ] ]
]]></Example>
<Example><![CDATA[
gap> cand := FrattiniFactorCandidates( 6, rec() );
[ rec( code := 25, extdim := [  ], first := [ 1, 2, 3 ], 
      isFrattiniFree := true, isUnique := true, order := 6, socledim := [ 1 ] 
     ), 
  rec( code := 1, extdim := [  ], first := [ 1, 1, 3 ], 
      isFrattiniFree := true, isUnique := true, order := 6, 
      socledim := [ 1, 1 ] ) ]
gap> FrattiniExtensions( cand, 12 );
[ rec( code := 6442, extdim := [ 2 ], first := [ 1, 2, 3 ], 
      isFrattiniFree := false, isUnique := true, order := 12, 
      socledim := [ 1 ] ), 
  rec( code := 266, extdim := [ 2 ], first := [ 1, 1, 3 ], 
      isFrattiniFree := false, isUnique := true, order := 12, 
      socledim := [ 1, 1 ] ) ]
]]></Example>
</Description>
</ManSection>
</Section>
<Section Label="Verifying non-isomorphism">
<Heading>Verifying non-isomorphism</Heading>
The output of the functions <C>FrattiniExtensionMethod</C> or <C>FrattiniExtensions</C>
might contain sublists of groups. That means that the groups contained
in sublists could not be distinguished up to isomorphism by the Frattini
extension method. However, the groups have gone through the random
isomorphism test and hence it is likely that they are not isomorphic.
<P/>
Here we provide a tool that can be used to try to prove that these
groups are non-isomorphic. This is not done automatically within
the Frattini extension method, since it might be time-consuming
and many users might not be interested in a complete verification
of non-isomorphism.
<P/>
To distinguish groups we compute invariants of the given groups.
Clearly, if the invariants differ, then we obtain that the corresponding
groups are not isomorphic. However, the converse is not true and hence
we might not succeed in distinguishing all non-isomorphic groups in a
given list. See <Cite Key="BE99"/> for a description of the invariants used.
<ManSection>
<Func Name="DistinguishGroups" Arg="list, bool"/>
<Description>
The function <C>DistinguishGroups</C> takes as input <A>list</A> a list as
described for the output of <C>FrattiniExtensions</C>. It returns a similar
list, where the sublists contained in <A>list</A> are split up.
<P/>
There are two levels to operate the function <C>DistinguishGroups</C> which
are controlled by the second input parameter <A>bool</A> of the function.
If <A>bool</A> is <K>false</K>, then only a few invariants are computed; if it is
<K>true</K>, then we also try the more complicated invariants.
Clearly, if <A>bool</A> is <K>false</K>, then the result is obtained faster,
but if <A>bool</A> is <K>true</K>, then we might distinguish more groups.
<P/>
If <C>DistinguishGroups</C> fails to split up the input list completely,
then a user might use the general-purpose function <C>IsomorphismGroups</C>
to prove the non-isomorphism between the remaining groups. However,
this might be a time-consuming computation.
</Description>
</ManSection>
</Section>
</Chapter>
